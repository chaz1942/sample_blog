%ul.nav.nav-tabs
  %li=link_to tag_icon(:list, pat(:list)), url(:register, :/)
  %li.active=link_to tag_icon(:plus, pat(:new)), url(:register, :/)

.tabs-content
  = form_for :account, url(:accounts, :create), :class => 'form-horizontal' do |f|
    =partial '/form', :locals => { :f => f }