.post{:class => "col-xs-6 col-lg-4"}
  %h2.title= link_to post.title, url_for(:posts, :show, :id => post)
  .date= time_ago_in_words(post.created_at || Time.now) + '以前'
  .body
  %p #{simple_format(post.body).slice!(0,50)}